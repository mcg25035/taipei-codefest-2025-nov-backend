# Simple Express Server

This is a tiny Express server that listens on port 5121 and responds with "hello world" to GET /.

Quick start:

```bash
cd /media/codingbear/code/codefest2025-nov
npm install
npm start
# then visit http://localhost:5121/ or run: curl http://localhost:5121/
```
